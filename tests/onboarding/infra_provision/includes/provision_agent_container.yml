  - env: prod
    install:
      - os_type: linux
        copy_files:
            - name: copy-agent-docker-compose
              local_path: tests/onboarding/autoinjection/docker/docker-compose-agent-prod.yml

        command: |
          echo "Who is using apt >>:"
          sudo ps aux | grep -i apt
          cat docker-compose-agent-prod.yml