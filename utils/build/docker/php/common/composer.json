{
    "name": "php/weblog",
    "type": "project",
    "require": {
        "weblog/acme": "*",
        "monolog/monolog": "*",
        "stripe/stripe-php": "^10.0"
    },
    "repositories": [
        {
            "type": "path",
            "url": "./Acme",
            "options": {
                "symlink": true
            }
        }
    ]
}
