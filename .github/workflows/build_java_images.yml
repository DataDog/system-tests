#DON'T EDIT THIS FILE. Autogenerated by utils/scripts/generate-github-action-file.py
name: Testing the test
'on':
  workflow_dispatch: {}
  pull_request:
    branches:
    - '**'
  push:
    branches:
    - main
env:
  REGISTRY: ghcr.io
jobs:
  build_java_image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout system tests
        uses: actions/checkout@5a4ac9002d0be2fb38bd78e4b4dbde5606d7042f # 2.3.4
        with:
          repository: 'DataDog/system-tests'
          path: './system-tests'

      - name: Set up QEMU
        uses: docker/setup-qemu-action@8b122486cedac8393e77aa9734c3528886e4a1a8 # 2.0.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@dc7b9719a96d48369863986a06765841d7ea23f6 # 2.0.0
        with: 
          install: true
          config-inline: |
            [worker.oci]
              max-parallelism = 1

      - name: Log in to the Container registry
        uses: docker/login-action@49ed152c8eca782a232dede0303416e8f356c37b # 2.0.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build java build image
        run: ./system-tests/utils/build/docker/java/spring-native_build/build.sh 