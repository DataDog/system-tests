# Lock npm registry
registry=https://registry.npmjs.org/

# Exact package versions only
save-exact=true

# Disable automatic package updates
audit=false
fund=false

# Use production dependencies only in production
production=true

# Disable package-lock generation during install (we have pre-generated one)
package-lock=false

# Speed up installs
prefer-offline=true
